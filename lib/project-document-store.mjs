import fs from 'node:fs'
import path from 'node:path'

export class FroamStaleRevisionError extends Error { constructor(expected, actual) { super(`Stale project revision: expected ${expected}, current ${actual}`); this.name = 'FroamStaleRevisionError'; this.expected = expected; this.actual = actual } }
const emptyRecord = (id) => ({ version: 2, id, revision: 0, sequence: 0, events: [], checkpoints: {}, branches: {} })
const normalizeRecord = (value, id) => ({ ...emptyRecord(id), ...(value ?? {}), version: 2, id, revision: Number.isSafeInteger(value?.revision) ? value.revision : 0 })
export function createMemoryProjectDocumentStore() {
  const projects = new Map(); const blobs = new Map(); const queues = new Map(); const serialized = (id, work) => { const prior = queues.get(id) ?? Promise.resolve(); const next = prior.catch(() => undefined).then(work); queues.set(id, next); const cleanup = () => { if (queues.get(id) === next) queues.delete(id) }; void next.then(cleanup, cleanup); return next }
  return { kind: 'memory', atomic: true, async read(id) { return projects.has(id) ? structuredClone(normalizeRecord(projects.get(id), id)) : null }, async compareAndSwap(id, expectedRevision, next) { return this.transaction(id, expectedRevision, () => next) }, async transaction(id, expectedRevision, update) { return serialized(id, async () => { const current = structuredClone(normalizeRecord(projects.get(id), id)); if (expectedRevision !== undefined && expectedRevision !== current.revision) throw new FroamStaleRevisionError(expectedRevision, current.revision); const stored = { ...structuredClone(await update(current)), version: 2, id, revision: current.revision + 1 }; projects.set(id, stored); return structuredClone(stored) }) }, async getBlob(id) { return blobs.get(id) ?? null }, async putBlob(id, value) { if (!blobs.has(id)) blobs.set(id, structuredClone(value)); return id } }
}
export function createFileProjectDocumentStore(file) {
  const queues = new Map(); const readAll = () => { try { const parsed = JSON.parse(fs.readFileSync(file, 'utf8')); return { version: 2, projects: parsed.projects ?? {}, blobs: parsed.blobs ?? {} } } catch { return { version: 2, projects: {}, blobs: {} } } }; const writeAll = (all) => { fs.mkdirSync(path.dirname(file), { recursive: true }); const temporary = `${file}.${process.pid}.${Date.now()}.tmp`; fs.writeFileSync(temporary, JSON.stringify(all) + '\n'); fs.renameSync(temporary, file) }; const serialized = (id, work) => { const prior = queues.get(id) ?? Promise.resolve(); const next = prior.catch(() => undefined).then(work); queues.set(id, next); const cleanup = () => { if (queues.get(id) === next) queues.delete(id) }; void next.then(cleanup, cleanup); return next }
  return { kind: 'file', atomic: true, async read(id) { const value = readAll().projects[id]; return value ? structuredClone(normalizeRecord(value, id)) : null }, async compareAndSwap(id, expectedRevision, next) { return this.transaction(id, expectedRevision, () => next) }, async transaction(id, expectedRevision, update) { return serialized(id, async () => { const all = readAll(); const current = structuredClone(normalizeRecord(all.projects[id], id)); if (expectedRevision !== undefined && expectedRevision !== current.revision) throw new FroamStaleRevisionError(expectedRevision, current.revision); const stored = { ...structuredClone(await update(current)), version: 2, id, revision: current.revision + 1 }; all.projects[id] = stored; writeAll(all); return structuredClone(stored) }) }, async getBlob(id) { return readAll().blobs[id] ?? null }, async putBlob(id, value) { return serialized(`blob:${id}`, () => { const all = readAll(); all.blobs[id] ??= value; writeAll(all); return id }) } }
}
export function adaptLegacyProjectStorage(storage) { if (storage?.transaction && storage?.read) return storage; if (!storage?.get || !storage?.put) throw new Error('[froam] invalid project document store'); return { kind: 'legacy', atomic: false, async read(id) { const value = await storage.get(id); return value ? normalizeRecord(value, id) : null }, async transaction(id, expectedRevision, update) { const current = normalizeRecord(await storage.get(id), id); if (expectedRevision !== undefined && expectedRevision !== current.revision) throw new FroamStaleRevisionError(expectedRevision, current.revision); const next = { ...await update(current), version: 2, id, revision: current.revision + 1 }; await storage.put(next); return next }, async getBlob(id) { return storage.getBlob ? storage.getBlob(id) : null }, async putBlob(id, value) { if (!storage.putBlob) throw new Error('Blob storage unsupported'); return storage.putBlob(id, value) } } }
